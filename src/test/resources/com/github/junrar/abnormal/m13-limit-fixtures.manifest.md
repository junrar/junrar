# M1.3 (issue #17) limit fixtures — provenance & UnRAR oracle

Generated by `generate_m13_limit_fixtures.py` (same directory), which byte-patches two
committed legitimate archives. Corrupt-variant class per PARITY_PLAN.md §4.3(1). No
compressor or rar/unrar binary is committed. Oracle = UnRAR 7.23 (WinRAR macOS tarball,
`unrar x -y -p-`); junrar must match the observable behaviour, not the internal path.

| Fixture | SHA-256 | Source (SHA-256) | Mutation | Guard exercised |
| --- | --- | --- | --- | --- |
| `filter-flood.rar` | `f6f56417fc0e54d57454eb42bc67dec1df05bb403a59baca33dacc1dc53393f0` | `tika-documents.rar` (`466e4703…621330`) | file bits 1892–1925: FiltPos ReadData → 32-bit form `0xFFFFFFFF` (bytes 234–238 `db61d00081`→`db61dfffff`) | unsigned FiltPos > Filters.size() (unpack30.cpp:383) |
| `channel-flood.rar` | `e656f0ddb8a4ab390582f0862266f2ecaf51f510f33eae2f3e9b06215e612938` | `tika-documents.rar` (`466e4703…621330`) | DELTA blob re-encoded with `InitR[0]=0x7FFFFFF0` channels (+4 bytes; owner FILE header packSize 2805→2809, header CRC refixed) | VMSF_DELTA channels > MAX3_UNPACK_CHANNELS 1024 (rarvm.cpp:203) |
| `flagsplace-oob.rar` | `4a02fee76fdc06fc92fea6484da23c1b51113782817f5d6e1cbbdb2db63eff20` | `audio/BoatModernEnglish-regular-unpack15-dos.rar` (`9c1deb8e…720a98`) | packed bytes 64–65 `0f92`→`fff0` → first GetFlagsBuf FlagsPlace == 256 | RAR1.5 FlagsPlace ≥ ChSetC.length (unpack15.cpp:412) + LongLZ ChSetB write-back mask (unpack15.cpp:291) |

## UnRAR 7.23 observed behaviour (all three)

`unrar x -y -p-` exits `3`; the byte-patched member fails with `- checksum error`; every
sibling member extracts `OK`; summary `Total errors: 1`.

- `filter-flood.rar` / `channel-flood.rar`: `test-documents/testEXCEL.xls - checksum error`, 9 siblings OK.
- `flagsplace-oob.rar`: `BOATMO~1.WAV - checksum error`, `LICENSE.TXT` OK.

junrar match: the corrupt member throws a typed `CrcErrorException`, siblings extract
normally, extraction returns immediately — asserted across all four public surfaces in
`UnpackLimitHostileArchiveTest`. On the pre-M1.3 base commit each fixture instead escapes a
raw `IndexOutOfBoundsException` / `ArrayIndexOutOfBoundsException` / `NegativeArraySizeException`.
